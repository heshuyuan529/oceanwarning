# 风险预警平台实现计划

> 版本：v1.0
> 成稿日期：2026-09-13
> 依据文档：[overallreadme.md](./overallreadme.md)（项目总览）、[chapter4.md](./chapter4.md)（风险预警章节）、风险预警全流程图
> 负责范围：仅风险预警平台（AI 复盘中心已实现，本计划只做衔接，不改动其功能）

---

## 1. 背景与目标

### 1.1 背景

企业真实数据尚未提供，LightGBM 训练、概率校准、时间外验证等模型工作暂无法开展。组长要求先完成平台侧能实现的部分。依据 chapter4 表4-11 实施路线，"阶段三：五阶段风险卡原型"与"阶段四：处置留痕台账"本身即为不依赖训练数据的平台交付物。

### 1.2 目标

将风险预警链路实现为**可用演示数据 + 专家先验驱动的可交互原型**，覆盖 chapter4 定义的核心业务流程：

```
任务快照 → 字段分流审计 → ML原生风险(mock) → 融合准入校验 → 软证据接入BN
→ 五阶段+总风险推断 → 单票风险卡 → 三档处置闭环 → 移交AI复盘中心
```

待企业数据到位后，仅替换 pML 的 mock 接口为真实模型输出，平台结构不变。

---

## 2. 现状分析

### 2.1 现有代码结构

| 文件 | 内容 |
|---|---|
| `src/main.tsx`（190 行） | 单文件 React 应用 = AI 复盘中心 MVP。4 个 Tab（案件总览/事件时间轴/Agent 会诊/人工复核），演示数据硬编码（青岛→鹿特丹冰箱整柜延误 15 天，CASE-2026-0712） |
| `src/styles.css` | 单行压缩 CSS，深蓝侧边栏 + 浅色内容区 |
| `src/vite-env.d.ts` | Vite 类型声明 |

### 2.2 技术约束

- 无路由库（useState 切 Tab）、无状态管理库、无后端
- 图标库 lucide-react 已引入，无需新增依赖
- 演示案例与文档 6.8 竞赛演示闭环一致，**预警主案例直接复用"青岛→鹿特丹"，天然形成预警→复盘闭环**

---

## 3. 范围界定

### 3.1 判断标准

是否依赖企业真实数据与模型训练结果。

### 3.2 能做与不能做清单

| 流程环节 | 依赖企业数据 | 本期实现 |
|---|---|---|
| LightGBM 训练、贝叶斯优化调参、概率校准 | 是 | ❌ 留接口与展示位 |
| P(M\|G) 桥接估计、时间外验证、PR-AUC/Brier 真实计算 | 是 | ❌ 留验证报告框架（演示数据填充） |
| 影子运行、上线准入 | 是 | ❌ 仅台账/界面框架 |
| 贝叶斯网络（DAG + CPT + 后验推断） | **否**（S3/S4 本就用专家先验启动，见 chapter4 表4-8） | ✅ 核心实现 |
| 快照管理、shipment_key、feature registry | 否（结构+规则） | ✅ 实现 |
| 单票风险卡、证据管理、处置闭环、复盘衔接 | 否（业务流程界面） | ✅ 实现（演示数据） |

### 3.3 明确不做（等企业数据）

1. LightGBM 训练 / Platt-Isotonic 校准 / 贝叶斯优化调参
2. P(M|G) 桥接的真实时间外估计
3. 分层消融验证（无M / 仅M / M+EBN）的真实计算
4. 影子运行真实运营

以上位置在平台中保留展示框架与"演示数据"状态标识（依据文档 6.8 要求：界面须区分"演示数据/模型结果/已核验事实/人工签发"）。

---

## 4. 目标文件结构

```
src/
├── main.tsx              # 入口：双工作台导航壳（风险预警 / AI复盘中心）
├── styles.css            # 现有样式 + 预警页面新样式（追加新块，不重排旧块）
├── types.ts              # Shipment / RiskCard / BnNode / Evidence 等类型定义
├── bn/
│   └── engine.ts         # 贝叶斯网络推断引擎（纯 TS，无第三方依赖）
├── data/
│   ├── shipments.ts      # 6 票演示任务 + pML/SHAP/快照 mock
│   ├── network.ts        # DAG 结构 + 专家先验 CPT + P(M|G) 桥接表
│   ├── evidence.ts       # 证据库 + 外部事件 + feature registry
│   └── review.ts         # 现有复盘演示数据（从 main.tsx 迁出，内容不改）
├── pages/
│   ├── warning/          # 新增：风险预警工作台（4 页）
│   │   ├── Workbench.tsx     # W1 预警工作台（任务列表）
│   │   ├── RiskCard.tsx      # W2 单票风险卡（核心页）
│   │   ├── Network.tsx       # W3 网络与证据治理
│   │   └── Ledger.tsx        # W4 处置台账
│   └── review/           # 现有 4 页原样迁入
│       ├── Overview.tsx      # 案件总览
│       ├── Timeline.tsx      # 事件时间轴
│       ├── Agents.tsx        # Agent 会诊
│       └── Review.tsx        # 人工复核
└── components/
    └── ui.tsx            # 共享小组件（Metric/Tag/Panel 等，从 main.tsx 抽出）
```

### 导航结构

```
风险预警
  ├ 预警工作台（W1）
  ├ 风险卡详情（W2，点击列表行进入）
  ├ 网络与证据（W3）
  └ 处置台账（W4）
AI 复盘中心
  ├ 案件总览
  ├ 事件时间轴
  ├ Agent 会诊
  └ 人工复核
```

---

## 5. 详细实施步骤

### 第 1 步：无损重构（不改功能）

**任务：**
1. 将 `main.tsx` 拆分为 `pages/review/*`（4 个页面组件）+ `components/ui.tsx`（Meta/Metric/PanelHead/Tag/Status/Message）+ `data/review.ts`（events/agents/responsibility 演示数据）
2. `main.tsx` 改造为双工作台导航壳：侧边栏分"风险预警 / AI 复盘中心"两组，复盘中心 4 页行为与现状完全一致
3. 预警组导航项先置灰占位（后续步骤逐页点亮）

**产出：** 重构后的目录结构，复盘中心功能与外观无变化。

**验证：** `pnpm build` 通过；手动走查复盘中心 4 页无回归。

---

### 第 2 步：BN 推断引擎（bn/engine.ts）

**任务：**
1. 节点模型定义：`{ id, label, states[], parents[], cpt: number[][], source: '数据估计'|'更新后验'|'专家先验'|'外部参考' }`；CPT 每行对应一种父节点状态组合，行内概率和为 1（加载时断言校验，误差 < 1e-6）
2. 推断算法：**枚举法精确推断**。演示网络约 12 个节点、每节点 2–3 状态，联合组合数约 7 万量级，毫秒级完成，无需引入推理库
3. 三种证据输入：
   - **硬证据**：观测节点状态过滤（如"舱位缺口=是"）
   - **软证据**：M 对 G 的似然 P(M|G) 虚拟证据（Ben Mrad 2015 桥接方式，M 取低/中/高三箱）
   - **干预模拟**：将可控节点强制为"改善"状态重算 → 情景对比（对应 chapter4 5.6.3 措施情景比较）
4. 输出：各节点后验分布、五阶段 S1–S5 后验、总风险 G 后验、证据加入前后变化量

**自验要求：**
- CPT 行和 = 1
- 后验归一化（每节点后验和 = 1）
- 软证据单调性：M=高 时 G 高风险后验应高于 M=低 时
- 硬证据单调性：观测"错过截港=是"后 G 后验上升

**产出：** `bn/engine.ts`，含推断、干预、校验函数。

**验证：** 临时脚本跑 4 项自验断言 + `pnpm build`。

---

### 第 3 步：演示数据层

**任务：**
1. `data/network.ts`——贝叶斯网络结构（依据 chapter4 表4-7、4.6 节）：
   - 主传导链：生产准备偏差 → 舱位缺口 → Booking确认异常 → 操作时间不足 → 报关资料不齐 → 错过截港 → G 总风险
   - 共同原因节点：港口拥堵、恶劣天气同时指向 S4/S5（不将两个结果互连）
   - 阶段汇总节点：S1 订单准备 / S2 订舱资源 / S3 拖车背箱 / S4 报关集港 / S5 离港出运，状态为"正常/关注/高风险"
   - G 状态：低/中/高；每节点直接父节点 ≤ 2–3 个
   - CPT 概率来源四级标识（数据估计/更新后验/专家先验/外部参考），**S3/S4 标"专家先验"+低可信度**（符合文档数据边界）
   - P(M|G) 桥接表：3×3，参数可配置，标注"演示值，待真实估计"
2. `data/shipments.ts`——6 票演示任务，覆盖全部业务状态：

| # | 场景 | 状态组合 | 处置路由 |
|---|---|---|---|
| 1 | 主案例：青岛→鹿特丹冰箱整柜 | pML 0.71，S2 舱位缺口+Booking未确认证据，距截港 72h，高可信 | 高风险高可信 → 跨部门会诊 |
| 2 | 高风险低可信 | S3/S4 无回执，Ci 低 | 补证 + 人工核验 |
| 3 | 中风险 | 岗位限时确认关键状态 | 中风险 → 岗位确认 |
| 4 | 低风险 | 常规监控 | 低风险 → 常规监控 |
| 5 | 数据不足票 | 关键字段缺失，显示"信息不足"（**不得显示为低风险**） | 补录数据 |
| 6 | 双模型冲突票 | ML高BN低（表5-9 组合二） | 检查证据缺失 |

   每票字段：shipment_key、港对、箱型箱量、计划 ETD、快照时点（T-7 主）、pML、SHAP Top3（业务语言转译）、G 后验（引擎实时计算）、五阶段后验（引擎实时计算）、证据列表、可信度 Ci、模型版本、处置状态。
3. `data/evidence.ts`——证据库：过程回执（Booking 确认、人工核验）、外部事件表（发布日期/影响起止/港口航线/等级/来源链接），每条含"业务发生时间 + 系统写入时间"；feature registry 约 15 个字段带 source_used_by 标记（ML/BN/only_future/audit_only）

**产出：** 3 个数据文件，全部类型化。

**验证：** `pnpm build`；类型检查通过。

---

### 第 4 步：W1 预警工作台

**布局：**
- 顶部统计条：各风险等级告警数量 vs 人工可处理容量（进度条）
- 任务列表表格：shipment_key / 港对 / 船期 / 箱型 / 距截港倒计时 / pML / G 后验 / 风险等级 / Ci / 处置状态
- **按 G 后验降序排列**（进入优先核查队列）
- 双模型冲突标记（表5-9 四种组合徽标：ML高BN高→优先处置；ML高BN低→查证据缺失；ML低BN高→按硬规则复核；双低→常规监控）
- 筛选器：风险等级 / 阶段 / 证据状态 / 冲突状态
- "信息不足"独立状态显示，样式与低风险明确区分

**交互：** 点击行进入 W2 风险卡。

**验证：** 6 票数据正确渲染；G 后验来自引擎实时计算而非硬编码。

---

### 第 5 步：W2 单票风险卡（核心页，工作量最大） ✅ 已完成

**布局：严格按 chapter4 表4-10 六大区域**

| 区域 | 内容 | 交互 |
|---|---|---|
| ① 总体预警 | pML、校准状态、G 后验、风险等级、有效期、模型/校准器版本 | — |
| ② 五阶段定位 | S1–S5 后验概率、等级、证据状态、可信度 Ci；**四种标签区分：已发生事实/已观察异常迹象/模型推断风险升高/数据不足** | 点击阶段查看该阶段证据 |
| ③ 证据清单 | 直接记录、M软证据、外部事件、专家先验、未知项；每条带形成时间、来源、"是否已被 ML 使用"标记 | 核验勾选 |
| ④ 主要路径 | 后验提升显著、时间靠前且可干预的传导链（如 S2舱位缺口→Booking确认异常→S3拖车时间不足→错过截港→G） | 路径高亮 |
| ⑤ 措施与时限 | 处置建议（备选舱位/补资料/确认车辆/加急申报）、最迟时限、责任岗位、**情景模拟对比值**（如"补充临约舱位后 G 0.72→0.41"） | 触发 BN 干预模拟实时计算 |
| ⑥ 处置动作区 | 按 4.9 三档处置逻辑路由按钮 | 见下方处置路由 |

**处置路由（chapter4 4.9 三档逻辑）：**
- 高风险 + 高可信 Ci → 「发起跨部门会诊」（最迟干预时间前）
- 高风险 + 低可信 → 「发起补证/人工核验」，**不得断言拖车报关已异常**
- 中风险 → 「岗位限时确认」
- 低风险 → 「常规监控」，**不得覆盖强制规则**（环境重大事件/合同红线走独立硬规则通道）

**底部：** 「冻结快照并移交 AI 复盘中心」按钮 → 跳转复盘中心。

**验证：** 情景模拟数值由引擎实时计算；处置动作写入台账（与 W4 联动）；主案例移交按钮可跳转复盘中心。

**完成记录（2026-09-15）：**
- `src/pages/warning/RiskCard.tsx` 创建完成，六区域全部实现
- ① 总体预警：风险等级徽章 + G 后验三态分布条形图（低/中/高）+ pML + Ci + 模型版本 + 防泄漏警告
- ② 五阶段定位：S1-S5 卡片网格，三态后验条形图，高亮主要风险阶段（P(高风险)>0.3 或 关注+高风险>0.5）
- ③ 证据清单：全证据表格（8 列），四种标签分类，防泄漏检查（visibleAtSnapshot），图例说明
- ④ 主要风险路径：buildRiskPath() 从硬证据构建根因→G 传导链，按 DAG 拓扑序排列，异常节点高亮
- ⑤ 措施情景模拟：buildMeasures() 生成可控节点干预选项，compareScenario() 实时计算基线 vs 干预后 P(G=高) 降幅
- ⑥ 处置动作：5 种路由配置（跨部门会诊/补证核验/岗位限时确认/常规监控/补录数据），责任到岗+时限约束，移交 AI 复盘中心按钮
- `src/main.tsx` 启用 riskcard 导航，替换占位为 RiskCard 组件，添加 onTransfer 移交逻辑
- `src/styles.css` 追加 rc-* 全部样式（①-⑥ 区域，约 115 行）
- `pnpm build` 通过（1830 模块，2.79s）
- 浏览器走查 10 项全部 PASS：主案例/冲突案例/信息不足案例三场景均正确渲染

---

### 第 6 步：W3 网络与证据治理 ✅ 已完成

**布局（三个子块）：**
1. **DAG 可视化**：CSS/SVG 手绘分层图（不引入图形库），主链 + 共同原因节点；每条边可查看业务说明、时间依据、例外情形
2. **CPT 管理**：节点列表 → 查看条件概率表（行和校验、来源四级标识、版本号）；支持编辑概率值（编辑后版本号 +1，保留旧版本）
3. **融合准入与模式开关**：
   - 五条件 checklist（chapter4 4.5.2）：校准完成 / 字段分流审计通过 / P(M|G) 已估计 / 融合不劣于单模型 / 风险卡可显示版本
   - **模式开关**：融合模式 ⇄ 降级模式（pML 仅用于排序不进网络，ML 与 BN 并列展示）——对应流程图降级分支，演示价值高、实现成本低
4. **feature registry 表格**：字段 / source_used_by 标记 / 计算时间 / 来源系统 / 责任人；重复使用检查提示
5. **证据录入界面**：录入过程回执与外部事件，自动可见性检查（预测时点之后形成的证据标记"不可用于当次推断"）

**验证：** 切换降级模式后 W1/W2 的 G 后验来源同步变化；CPT 编辑触发重新推断。

**完成记录（2026-09-15）：**
- `src/pages/warning/Network.tsx` 创建完成，5 个子块全部实现
- ① DAG 可视化：7 层分层布局（独立原因→主链 4 层→阶段汇总→总风险），14 节点可点击查看 CPT，19 条边元数据可展开
- ② CPT 管理：条件概率表（行和校验、来源四级标识），节点快捷选择栏
- ③ 融合准入与模式开关：融合/降级模式切换（影响 W1/W2 BN 推断），五条件 checklist，P(M|G) 桥接表（降级模式下显示警告）
- ④ Feature Registry：16 字段分流审计表（ML 8/BN 6/only_future 1/audit_only 1），无重复使用校验
- ⑤ 证据录入：6 字段表单 + 描述 + 可见性检查（快照时点后标记不可用）+ 外部事件表
- `src/main.tsx` 启用 network 导航，添加 mode 状态提升至 App 级，传递 mode 到 Workbench/RiskCard/Network
- `src/pages/warning/Workbench.tsx` 接受 mode 属性，降级模式不使用软证据
- `src/pages/warning/RiskCard.tsx` 接受 mode 属性，降级模式不使用软证据
- `src/styles.css` 追加 net-*/dag-*/cpt-*/ev-* 样式（约 115 行）
- `pnpm build` 通过（1831 模块，2.60s）
- 浏览器走查 7 项 PASS：DAG 布局/CPT 切换/模式切换/Feature Registry/证据录入/边元数据均正确

---

### 第 7 步：W4 处置台账 + 闭环衔接 ✅ 已完成

**布局：**
1. 处置记录表：时间 / shipment_key / 处置类型 / 动作 / 责任岗位 / 时限 / 采纳与否 / 不采纳原因 / 结果 / 关联风险卡
2. 运营指标卡：告警总量、采纳率、补证率、可干预比例（chapter4 表4-9 影子运行指标框架，演示数据）
3. 强制规则记录区：硬规则触发与处置（模型低风险不得覆盖）

**闭环衔接：**
- 主案例风险卡「移交复盘」→ 跳转复盘中心案件总览
- 复盘中心案件总览增加「来源：风险预警」标记（最小改动）
- 处置/未处置记录作为复盘责任判定输入的说明文案（对应文档批注"处置了怎么追责，不处置怎么追责"）

**验证：** `pnpm build`；完整链路手动走查。

**完成记录（2026-09-15）：**
- `src/data/disposition.ts` 创建完成：11 条处置记录（覆盖 6 票任务）+ 6 个运营指标 + 2 条强制规则记录
- `src/pages/warning/Ledger.tsx` 创建完成，3 个区块全部实现：
  - ① 运营指标卡：告警总量/高风险告警/采纳率/补证率/可干预比例/平均响应时长
  - ② 处置记录台账：11 列表格，按路由筛选（5 种路由），「仅看未采纳」过滤器，关联风险卡可跳转
  - ③ 强制规则记录：2 条硬规则（目的港拥堵强制升级、航线拥堵指数监控），模型风险 vs 强制风险对比
- `src/pages/review/Overview.tsx` 更新：增加「来源：风险预警平台」蓝色横幅 + 建议责任分布区处置记录备注
- `src/main.tsx` 启用 ledger 导航，添加 transferredKey 状态，传递 fromWarning 到 Overview
- `src/types.ts` 修正 DispositionRecord 类型（rejectReason/result 允许 null）
- `src/styles.css` 追加 ld-*/ov-* 样式（约 40 行）
- `pnpm build` 通过（1833 模块，2.32s）
- 浏览器走查 6 项全部 PASS：台账三区块/筛选交互/闭环流转/来源横幅/处置备注/无视觉错误

**闭环验证结果：** 预警工作台 → 主案例风险卡 → 移交 AI 复盘中心 → 案件总览（含来源标记）→ 会诊 → 签发，全程贯通。

---

## 6. 端到端验证清单（最终验收）

| # | 验收项 | 通过标准 |
|---|---|---|
| 1 | 构建通过 | `pnpm build` 无错误，`tsc` 类型检查通过 |
| 2 | 复盘中心无回归 | 原有 4 页功能与外观不变 |
| 3 | BN 引擎正确性 | CPT 行和=1、后验归一化、软证据/硬证据单调性 4 项断言通过 |
| 4 | 主链路走查 | 工作台列表 → 主案例风险卡 → 发起会诊/补证 → 移交复盘中心 → 复盘签发，全程贯通 |
| 5 | 六票状态覆盖 | 高/低/中/数据不足/冲突各票展示与处置路由符合 4.9 逻辑 |
| 6 | 文档符合性 | 风险卡六区域、四标签区分、"信息不足≠低风险"、S3/S4 专家先验标识、降级模式均按 chapter4 落地 |
| 7 | 演示数据标识 | 界面明确区分"演示数据/模型结果/已核验事实/人工签发"（文档 6.8 要求） |

---

## 7. 里程碑与改动记录

| 步骤 | 内容 | 状态 | 完成日期 | 实际改动记录 |
|---|---|---|---|---|
| 1 | 无损重构 + 双工作台导航壳 | ✅ 已完成 | 2026-09-13 | 见下方记录 ① |
| 2 | BN 推断引擎 | ✅ 已完成 | 2026-09-15 | 见下方记录 ② |
| 3 | 演示数据层 | ✅ 已完成 | 2026-09-15 | 见下方记录 ③ |
| 4 | W1 预警工作台 | ✅ 已完成 | 2026-09-15 | 见下方记录 ④ |
| 5 | W2 单票风险卡 | ☐ 未开始 | — | — |
| 6 | W3 网络与证据治理 | ☐ 未开始 | — | — |
| 7 | W4 处置台账 + 闭环衔接 | ☐ 未开始 | — | — |

> 约定：每步完成后在此表更新状态、日期与实际改动记录（改动文件清单 + 关键变更点），保证全程可追溯。

### 改动记录 ①（第 1 步：无损重构，2026-09-13）

**新增文件：**

| 文件 | 说明 |
|---|---|
| `src/types.ts` | 全局类型。`ReviewTab` / `WarningTab` / `Tab` 三层导航类型（计划原定第 3 步创建，因导航壳需要提前建立，仅含导航类型，第 3 步再扩展领域类型） |
| `src/data/review.ts` | 复盘中心演示数据（events / agents / responsibility），自 main.tsx 原样迁出，内容零改动 |
| `src/components/ui.tsx` | 共享小组件 Meta / Metric / PanelHead / Tag / Status / Message + `AgentStatus` 类型，原样迁出 |
| `src/pages/review/Overview.tsx` | 案件总览页，原样迁出（props: `onNavigate` / `onStart`） |
| `src/pages/review/Timeline.tsx` | 事件时间轴页，原样迁出（含筛选逻辑） |
| `src/pages/review/Agents.tsx` | Agent 会诊页，原样迁出（props: `running` / `step` / `statusFor`） |
| `src/pages/review/Review.tsx` | 人工复核页，原样迁出（props: `signed` / `onSign`） |

**修改文件：**

| 文件 | 变更 |
|---|---|
| `src/main.tsx` | 由 190 行单文件应用改为双工作台导航壳：新增"风险预警"导航分组（4 项 disabled 置灰占位，title 提示待实施）+ "AI 复盘中心"分组；复盘 4 页改为组件引用；会诊动画逻辑保持不变 |
| `src/styles.css` | 末尾追加独立段落块"第 1 步追加"：`.nav-group` 分组标题、`.nav-item:disabled` 置灰样式；未重排任何旧内容 |

**有意变更（非迁移）：** 品牌副标题由"AI 复盘中心"改为"风险预警 · AI 复盘"，反映双工作台定位；header 区域（案件标题/导入材料/重新运行会诊按钮）保持原样。

**验证结果：** `pnpm build` 通过（tsc -b + vite build，1824 modules，3.82s）；浏览器走查 4 项全部通过——① 侧边栏分组与置灰项正常 ② 时间轴 10 行数据及"原生延误"筛选正常 ③ 会诊动画逐个点亮 6 卡片正常 ④ 责任比例、待补证标记、签发流程与 toast 正常；console 无报错。

### 改动记录 ②（第 2 步：BN 推断引擎，2026-09-15）

**新增文件：**

| 文件 | 说明 |
|---|---|
| `src/bn/engine.ts` | 贝叶斯网络精确推断引擎，纯 TypeScript 无依赖。含 6 个公开函数/类型：`BnNode` / `BnNetwork`（类型定义）、`validateNetwork`（CPT 行和/父节点引用/0-1 禁止/环检测校验）、`infer`（枚举法精确推断，支持硬证据+软证据+do 算子三种证据同时处理）、`compareScenario`（情景比较，基线 vs 干预后后验差异）、`selfTest`（5 项断言自验） |

**关键技术决策：**
- 算法选择**枚举法精确推断**而非变分推断或采样：演示网络 14 节点（8 个二态 + 6 个三态），联合空间约 18 万，JS 毫秒级完成，无需引入推理库
- CPT 行索引采用**混合基数编码**：第一个父节点为最高位，支持不等状态数的父节点组合
- do 算子实现：干预节点强制值并**移除 CPT 因子**（factor=1），区别于硬证据（保留 CPT 因子）——符合 Pearl 因果干预定义
- 软证据实现：对目标节点各状态乘以似然权重 P(M\|G)，等价于添加虚拟子节点——符合 Ben Mrad 2015 桥接方案
- 所有概率禁止 0 或 1（chapter4 要求 Beta/Dirichlet 先验 + 平滑），加载时断言校验

**自验结果（6 项全部 ✅）：**
1. ✅ CPT 行和 = 1（validateNetwork 通过，无错误）
2. ✅ 后验归一化（无证据时 G 后验和 = 1.000000000）
3. ✅ 软证据单调性（M 偏高→P(G=高)=0.3945 > M 偏低→P(G=高)=0.0562）
4. ✅ 硬证据单调性（B=是→P(G=高)=0.6000 > 基线 P(G=高)=0.1360）
5. ✅ 干预模拟（do(B=否)→P(G=高)=0.1000 < 基线 0.1360，do 算子正确移除因果因子）

**验证方式：** 临时 Vite dev server HTML 页面（`public/bn-test.html`）通过 ES module 导入引擎执行 selfTest，浏览器读取结果后删除临时文件。`pnpm build` 通过（tsc -b + vite build，1824 modules，2.80s）。

### 改动记录 ③（第 3 步：演示数据层，2026-09-15）

**新增文件：**

| 文件 | 说明 |
|---|---|
| `src/types.ts`（扩展） | 在第 1 步导航类型基础上，新增领域类型：`RiskLevel` / `EvidenceLabel`（四种标签）/ `DispositionStatus` / `DispositionRoute` / `ConflictType`（表5-9四组合）/ `DataStatus`（6.8状态标识）/ `Shipment` / `Evidence` / `ExternalEvent` / `FeatureRegistryEntry` / `DispositionRecord` / `EdgeMeta`，共 12 个接口 + 9 个联合类型 |
| `src/data/network.ts` | 14 节点贝叶斯网络（8 二态事件 + 5 三态阶段 + 1 三态总风险）；19 条 DAG 边元数据（含业务说明/时间依据/例外）；P(M\|G) 桥接表（3×3 演示值）；`getSoftEvidence()` 辅助函数；融合准入五条件 checklist（chapter4 4.5.2） |
| `src/data/shipments.ts` | 6 票演示任务覆盖全部处置路由：①主案例高风险高可信→会诊 ②高风险低可信→补证 ③中风险→岗位确认 ④低风险→常规监控 ⑤数据不足（pML=null）→补录 ⑥ML高BN低冲突→补证。每票含 SHAP Top3（业务语言转译，标注"预测贡献≠因果"） |
| `src/data/evidence.ts` | 13 条证据记录（process_receipt / external_event / unknown / expert_prior）关联 6 票任务；2 条外部事件（鹿特丹拥堵/美西拥堵）；16 条 feature registry 字段（ML 专用 8 个 + BN 专用 6 个 + only_future 1 个 + audit_only 1 个，**同一字段不重复**）；`extractHardEvidence()` 辅助函数 |

**关键技术决策：**
- DAG 主链：生产准备偏差→舱位缺口→Booking确认异常→操作时间不足/报关资料不齐→错过截港→G；共同原因节点（港口拥堵/恶劣天气）同时指向 S4/S5/G，**不将两个并发结果互连**（chapter4 要求）
- G 的父节点为 `[miss_cutoff, port_congestion, bad_weather]`（3 个，符合≤3 限制），而非 S1-S5（243 行 CPT 不可行）；S1-S5 为诊断读出节点，后验仍由推断引擎计算
- CPT 概率来源：S3/S4 标注"专家先验"（chapter4 数据边界），其余事件节点"数据估计"，外部节点"外部参考"；所有概率严格在 (0,1) 开区间
- P(M|G) 桥接表为演示值，标注"待真实估计"；每行和=1（合法条件分布）
- 数据不足票（SH-2026-0910）pML=null、mBin=null，后续界面将显示"信息不足"而非"低风险"（chapter4 4.6 硬性要求）
- 冲突票（SH-2026-0908）ML高BN低：BN 证据全正常但 ML 特征（船司历史延误率/航线拥堵指数）不在 BN 中——演示字段分流审计的价值

**网络校验 + 推断验证结果（全部 ✅）：**
1. ✅ `validateNetwork` 通过：14 节点、CPT 行和均=1、无 0/1 概率、无环
2. ✅ 无证据基线：G 后验归一化（和=1.000000000），P(G=低)=0.566 / P(G=高)=0.243
3. ✅ 主案例 SH-2026-0712（硬证据：prod_delay=是 + space_gap=有 + booking_anom=异常 + port_congestion=有；软证据：M=高）：G 后验 P(G=高)=**0.893**，五阶段后验 S1/S2 高风险=0.80、S3=0.67、S4=0.68、S5=0.73
4. ✅ 单调性：主案例 P(G=高)=0.893 > 基线 0.243
5. ✅ 低风险票 P(G=低)=0.846 > 主案例 P(G=低)=0.022

`pnpm build` 通过（tsc -b + vite build，1824 modules，2.17s）。临时验证文件已清理。

### 改动记录 ④（第 4 步：W1 预警工作台，2026-09-15）

**新增文件：**

| 文件 | 说明 |
|---|---|
| `src/pages/warning/Workbench.tsx` | 预警工作台页面：统计条（高/中/低/信息不足 vs 可处理容量进度条）+ 筛选器（5 档）+ 任务列表表格（11 列：shipment_key/港对/船期/箱型/距截港/pML/G后验P高/风险等级/Ci/冲突/处置状态）。G 后验由 `infer()` 实时计算，按 P(G=高) 降序排列，"信息不足"排末尾。行可点击跳转风险卡（W2 占位）。 |

**修改文件：**

| 文件 | 变更 |
|---|---|
| `src/main.tsx` | 默认 Tab 由 `overview` 改为 `workbench`；预警工作台导航项从 disabled 改为可点击（其余 3 项仍置灰）；header 改为动态（预警区显示"风险预警 · 演示数据"+对应标题，复盘区不变）；新增 `selectedKey` 状态 + riskcard 占位页；删除 `warningNavItems` 数组改为逐项声明 |
| `src/styles.css` | 末尾追加"W1 预警工作台"样式块（`.wb-stat-bar` / `.wb-table` / `.wb-risk` / `.wb-conflict` / `.wb-g-bar` / `.wb-placeholder` 等），不重排旧内容 |
| `src/data/evidence.ts` | 冲突票 SH-2026-0908 新增 4 条硬证据（EV-014~017：prod_delay=否/space_gap=无/booking_anom=正常/customs_incomplete=齐全），使 BN 有独立正常证据拉低后验，形成真正的"ML高BN低"冲突 |
| `src/data/shipments.ts` | SH-2026-0908 的 `hardEvidenceIds` 更新为包含 EV-014~017 |

**关键技术决策：**
- 风险等级由 G 后验实时计算（P(G=高)>0.5→高；P(G=中)+P(G=高)>0.35→中；否则→低；pML=null→信息不足），不硬编码
- "信息不足"使用灰色斜体样式 `.r-na`，与绿色"低风险" `.r-low` 视觉明确区分（chapter4 4.6 硬性要求）
- G 后验列使用渐变 mini-bar + 百分比数值（`.wb-g-bar`），直观展示风险排序
- 距截港倒计时从参考日期 2026-09-15 计算，已过截港显示灰色"已过X天"，≤2 天显示红色"X天"
- 冲突票修复：添加"全正常"硬证据后，SH-2026-0908 的 P(G=高) 从 >0.5 降至 0.415（中风险），正确呈现"ML高BN低"——演示字段分流审计价值（ML 用的船司历史延误率/航线拥堵指数不在 BN 中）

**浏览器走查结果（6 项全部通过）：**
1. ✅ 默认页为预警工作台，header 显示"风险预警 · 演示数据" + "预警工作台"
2. ✅ 统计条：高2 / 中2 / 低1 / 信息不足1，待处理 4/批次
3. ✅ 6 行按 G 后验降序排列，信息不足行灰色斜体样式
4. ✅ 筛选"高"仅显示 2 行，"全部"恢复 6 行
5. ✅ 点击行跳转占位页（"第 5 步实现"），返回按钮正常
6. ✅ 切换到复盘中心 header 变回"异常复盘案件"

`pnpm build` 通过（tsc -b + vite build，1829 modules，3.92s）。

---

## 8. 风险与注意事项

1. `styles.css` 为压缩单行格式：新样式**追加独立段落块**，不重排旧内容，降低合并冲突与回归风险
2. `body { min-width: 1100px }` 保持，预警页面按此宽度设计
3. 不新增任何 npm 依赖（无路由库、无图形库、无推理库），保持项目轻量
4. BN 演示 CPT 数值需符合业务单调性（如"操作时间不足=是"时 S3 高风险后验必须升高），避免演示时出现业务上不合常理的反向结果
5. 所有模型相关展示必须带版本号与"演示数据"标识，防止评审误认为已接入真实模型
