# newest.md 与现有代码差异分析及修改方案

> 版本：v1.0
> 成稿日期：2026-09-16
> 依据文档：[newest.md](./newest.md)（新版第三、四章）与现有 [risk-warning-implementation-plan.md](./risk-warning-implementation-plan.md)（旧版实现）
> 范围：仅风险预警平台（AI 复盘中心不改动）

---

## 1. 第三章差异（算法层 — 重大变化）

### 1.1 贝叶斯网络结构完全不同

| 维度 | 旧版（当前代码） | 新版（newest.md） |
|---|---|---|
| G 节点 | 3 态（低/中/高） | **2 态（晚开=1, 未晚开=0）** |
| 事件节点 | 8 个（prod_delay, space_gap, booking_anom, time_insuf, customs_incomplete, miss_cutoff, port_congestion, bad_weather） | **无事件节点** |
| 阶段节点 | S1-S5 在 BN 中作为后验推断节点 | **S1-S5 不在 BN 中推断**，仅用于流程定位和证据组织 |
| DAG 规模 | 14 节点、19 条边 | **极简结构**：C（ML 分位区间）→ G ← S（生产付款联合状态），V 为虚拟证据 |
| CPT 来源 | 专家先验（手工指定） | **历史频数估计** + Laplace 对称平滑（0.5） |

关键引用（newest.md 第 889 行）：
> "第三章输出的是订单层面的最终风险概率，**而非五阶段分别的后验概率**；五阶段节点用于标识订单所处环节、事实异常与证据缺口，**不将其表述为本轮已完成验证的阶段后验概率**。"

### 1.2 证据模型完全不同

| 维度 | 旧版 | 新版 |
|---|---|---|
| 证据类型 | 硬证据（事件节点观测）+ 软证据（P(M\|G) 桥接表） | **生产与付款联合状态 S**（5×5 = 最多 25 种组合） |
| ML 接入方式 | P(M\|G) 桥接表，3 箱（低/中/高）映射为 G 似然 | **虚拟证据**：λ = q/π（ML 概率除以网络先验） |
| 桥接表 | 有 `bridgingTable`（3×3） | **无 P(M\|G) 桥接表**，改为条件频数表 θ(g,s) |

新版 5 状态定义（newest.md 表 3-7）：

| 状态 | 判定规则 | 解释 |
|---|---|---|
| 已完成未逾期 | 实际记录可见且实际日期不晚于计划日期 | 已观察到按约定完成 |
| 已完成逾期 | 实际记录可见且实际日期晚于计划日期 | 已观察到完成但发生计划偏差 |
| 已到期未观测 | 计划日期次日已到达快照但实际记录不可见 | 需要核实到期后的完成状态 |
| 未到期未观测 | 计划尚未到期且实际记录不可见 | 处于等待完成或记录出现的状态 |
| 计划未知 | 计划缺失或不在有效日期范围 | 无法按最初约定判定是否逾期 |

### 1.3 融合方式不同

| 维度 | 旧版 | 新版 |
|---|---|---|
| 融合方法 | 仅一种（P(M\|G) 桥接） | **两种**：条件融合（按 ML 概率分位数分层）+ 不分层融合 |
| 校准 | 无 | **两阶段校准**：第一次（ML→贝叶斯输入尺度 q），第二次（融合输出 r→最终概率 p*） |
| 后验计算 | 枚举法精确推断 | **闭式后验**：r = q·LR / (1-q + q·LR)，LR = P(S\|G=1,C)/P(S\|G=0,C) |

### 1.4 数据基础不同

| 维度 | 旧版 | 新版 |
|---|---|---|
| 数据量 | 6 票演示任务 | **141,971 条订单**（2025.8-2026.7） |
| 预测目标 | 模糊的"风险等级" | **订单晚开 > 24h**（严格定义） |
| 快照时点 | 单一快照 | **3 个提前期**（7天/3天/1天）各自独立快照 |
| 评价方式 | 无 | AP、Brier、命中率、召回率、航次重抽样 |

---

## 2. 第四章差异（平台层 — 中等变化）

### 2.1 处置路由从 5 种简化为 2×2 矩阵

旧版 5 种路由（跨部门会诊/补证核验/岗位限时确认/常规监控/补录数据）→ 新版 2×2（newest.md 表 4-1）：

| 风险概率 \ 可信度 | 高 | 低 |
|---|---|---|
| **高** | 启动跨部门处置 | 优先补证或人工核验 |
| **低** | 常规监控 | 补充数据后再判断 |

### 2.2 风险卡字段变化（newest.md 表 4-2）

新增字段：`p_raw`（原始概率）、`SHAP 主要贡献特征`、`桥接版本`
阶段后验字段含义变化：S1-S5 不再是"BN 后验概率"，而是"流程定位 + 证据缺口"

### 2.3 新增概念

- **处置任务状态机**（4.4 节）：待核验→已分派→处理中→待验证→已关闭 + 升级状态
- **剩余干预裕量公式**（4.5 节）：W = t_deadline - t_now - T_exec
- **处置效果双维评价**（4.6 节）：业务结果（是否追回时间）+ 过程履职（是否及时响应/升级）

### 2.4 旧版有但新版未提及的概念

| 旧版概念 | 新版状态 |
|---|---|
| 风险卡六区域布局（①-⑥） | 未明确要求，但字段仍可对应 |
| P(M\|G) 桥接表 | **删除** |
| 融合准入五条件 | **删除** |
| Feature Registry 字段分流审计 | **删除**（证据模型改变，不再需要分流） |
| 证据录入界面 | **删除**（证据通过日期规则自动计算） |
| 强制规则记录区 | **未提及** |
| 降级模式开关 | **删除**（ML 始终通过虚拟证据进入 BN） |

---

## 3. 需要修改的代码文件及内容

### 3.1 重大修改（算法层）

| 文件 | 修改内容 |
|---|---|
| `src/data/network.ts` | **重写**：删除 14 节点 DAG + P(M\|G) 桥接表，改为 G(2 态) + S(联合状态) + C(分位区间) 结构，CPT 改为历史频数估计 |
| `src/bn/engine.ts` | **重写推断逻辑**：删除枚举法，改为闭式后验 r = q·LR/(1-q+q·LR)；新增虚拟证据 λ=q/π；新增两阶段校准 |
| `src/types.ts` | G 节点从 3 态改为 2 态；新增生产付款联合状态类型；删除事件节点类型 |
| `src/data/shipments.ts` | 演示数据改为真实订单字段（p_raw, q, S 联合状态, SHAP 特征） |
| `src/data/evidence.ts` | 证据从"事件节点硬证据"改为"生产付款联合状态"；删除 featureRegistry |

### 3.2 中等修改（平台层）

| 文件 | 修改内容 |
|---|---|
| `src/pages/warning/RiskCard.tsx` | ①总体预警：G 改为 2 态后验，新增 p_raw/SHAP/桥接版本；②五阶段定位：从 BN 后验改为流程定位+证据缺口；⑤措施模拟：干预对象从事件节点改为生产/付款状态；处置路由从 5 种改为 2×2 |
| `src/pages/warning/Workbench.tsx` | 排序逻辑从 G(高) 改为最终概率 p*；处置路由简化 |
| `src/pages/warning/Network.tsx` | DAG 可视化重画（极简结构）；CPT 管理改为条件频数表；删除 P(M\|G) 桥接表和融合准入五条件 |
| `src/pages/warning/Ledger.tsx` | 处置路由简化为 4 种；新增任务状态机；新增剩余干预裕量 |
| `src/data/disposition.ts` | 处置记录路由调整；新增任务状态字段 |

---

## 4. 四个待确认问题的结论

### 问题 1：六区域风险卡布局

**结论：保留 UI 布局，调整各区域内容。**

- ①总体预警：新增 `p_raw`、`SHAP 主要贡献特征`、`桥接版本` 展示位；G 后验从 3 态改为 2 态
- ②五阶段定位：从"BN 后验概率"改为"流程定位 + 证据缺口"（不再展示 S1-S5 的后验条形图）
- ③证据清单：证据类型从"事件节点硬证据"改为"生产付款联合状态"
- ④主要风险路径：删除旧版事件节点传导链，改为"生产/付款状态 → G"的因果说明
- ⑤措施情景模拟：干预对象从事件节点改为生产/付款状态
- ⑥处置动作：从 5 种路由改为 2×2 矩阵

### 问题 2：Feature Registry 和证据录入界面

**结论：删除。**

- 删除 W3 的 Feature Registry 表格（"字段分流审计"概念已无意义）
- 删除 W3 的证据录入界面（证据通过日期规则自动计算，不需要手动录入）
- W3 该位置可改为"字段说明表"（展示 newest.md 表 3-1 的核心字段与算法用途）

### 问题 3：降级模式和融合准入

**结论：删除，不替换。**

- 删除 W3 的融合/降级模式开关（ML 始终通过虚拟证据进入 BN，不存在降级模式）
- 删除 W3 的融合准入五条件 checklist
- 不需要用"模型性能展示"来填充——AP/Brier/命中率/召回率是第三章实验评价结果，属于算法研究阶段产物，不是业务运营平台需要展示的内容
- W3 删除后保留三个子块：① DAG 可视化（新版极简结构）② 条件频数表管理（替代旧版 CPT 管理）③ 字段说明表（替代旧版 Feature Registry，展示 newest.md 表 3-1）

### 问题 4：真实数据展示

**结论：不展示原始订单数据，不展示实验指标。**

- 不展示 14 万条原始订单数据
- 不展示 AP/Brier/命中率/召回率/容量敏感性曲线等实验评价指标（研究产物，非平台功能）
- 在 W2 风险卡展示 `p_raw`、`SHAP 特征`、`模型版本`、`桥接版本`（表 4-2 要求的单票模型元数据）
- 演示数据中的模型版本、校准参数等信息可标注为"演示数据"

---

## 5. 修改优先级与建议顺序

| 顺序 | 修改项 | 影响范围 | 说明 |
|---|---|---|---|
| 1 | types.ts + network.ts + engine.ts | 算法核心 | 先改类型和网络定义，再改推断引擎 |
| 2 | shipments.ts + evidence.ts | 数据层 | 改演示数据为新版字段 |
| 3 | RiskCard.tsx | 核心页面 | 六区域内容调整 |
| 4 | Workbench.tsx | 工作台 | 排序逻辑和展示调整 |
| 5 | Network.tsx | 网络治理 | DAG 重画 + 删除旧功能 + 新增模型性能 |
| 6 | Ledger.tsx + disposition.ts | 处置台账 | 路由简化 + 任务状态机 |
| 7 | main.tsx + styles.css | 导航与样式 | 联动调整 |
| 8 | pnpm build + 走查 | 验证 | 全链路验证 |
